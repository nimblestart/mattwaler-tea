<template
  x-data="{ submitted: false }"
  x-init="submitted = window.location.search.includes('?contact')"
  x-if="submitted"
>
  <div class="p-24 bg-green-500 text-white font-700">
    <div class="container flex justify-center items-center">
      <p>
        Thanks for reaching out! I will respond to your message as soon as possible.
      </p>
      <button @click="submitted = false" class="w-24 flex-shrink-0 ml-24">
        {% include 'icons/close.svg' %}
      </button>
    </div>
  </div>
</template>

<div class="h-12 bg-indigo-500"></div>
<header>
  <div class="container px-24 pt-48 flex justify-center items-center">

    {# Logo #}
    <div class="flex items-center">
      <span class="w-64">
        {% include 'icons/whale-logo.svg' %}
      </span>
      <div class="ml-12 leading-none">
        <span class="block font-900 text-20">Matt Waler</span>
        <span class="text-14 block text-gray-600">Web Developer</span>
      </div>
    </div>

  </header>
