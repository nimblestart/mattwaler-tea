const buttonClasses =
  'text-sm px-4 py-2 rounded font-black uppercase tracking-wide'
const heading = 'font-black leading-tight'

module.exports = {
  button: `${buttonClasses} bg-indigo-500 text-white`,
  buttonAlt: `${buttonClasses} bg-gray-200 text-indigo-500`,
  container: 'max-w-screen-lg mx-auto px-8',
  h1: `${heading} text-3xl md:text-4xl`,
  h2: `${heading} text-2xl md:text-3xl`,
  h3: `${heading} text-xl md:text-2xl`,
}
