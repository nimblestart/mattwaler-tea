---
title: Projects
description: A listing of all the stuff I think is worth showing off.
hero:
  heading: What I have been working on.
  subheading: Here is a list of the projects that actually came to fruition. I am 99% sure the code for all of these are publicly available on my GitHub if you're itching to see some source code.
---

{% extends 'page.njk' %}

{% block body %}
  <section class="gradient-purple text-white py-8 lg:py-16">
    <div class="container max-w-screen-md mx-auto text-center">
      <h1 class="text-2xl sm:text-3xl xl:text-4xl font-bold leading-none">
        {{ hero.heading }}
      </h1>
      <p class="mt-4 text-sm sm:text-base text-indigo-100">{{ hero.subheading }}</p>
    </div>
  </section>
  <span class="w-full h-12 block gradient-purple text-gray-100">
    {% include 'icons/waves.svg' %}
  </span>

  <section class="container max-w-screen-lg mt-16">
    {% for item in collections.projects %}
      <article class="sm:flex items-center pt-12 mt-12 {{ 'border-t' if not loop.first }}">
        <div
          class="sm:w-1/3 h-64 bg-gray-400 bg-cover rounded-lg shadow-lg"
          style="background-image: url({{ item.data.image }})"
        ></div>
        <div class="mt-8 sm:ml-12 sm:mt-0 sm:w-2/3">
          <a target="_blank" href="{{ item.data.link }}">
            <h2 class="text-2xl sm:text-3xl font-extrabold leading-none">{{ item.data.title }}</h2>
            <p class="text-sm font-medium {{ item.data.color }} underline">{{ item.data.link }}</p>
          </a>
          <p class="text-base text-gray-700 mt-4">{{ item.data.description }}</p>
          <a
            target="_blank"
            href="{{ item.data.link }}"
            class="inline-flex items-center mt-6 {{ item.data.color }} font-bold"
          >
            <span class="leading-none">
              View Site
            </span>
            <span class="w-4 ml-1">
              {% include 'icons/arrow.svg' %}
            </span>
          </a>
        </div>
      </article>
    {% endfor %}
  </section>
{% endblock body %}
