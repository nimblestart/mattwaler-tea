---
title: Sites
description: A list of some of the sites I've graced my presence with.
hero:
  heading: Websites I have helped bring to life.
  subheading: Here is a list of some of the websites I've graced my presence with.
---

{% extends 'page.njk' %}

{% block body %}
  <section class="gradient-purple text-white py-8 lg:py-16">
    <div class="container max-w-screen-md mx-auto text-center">
      <h1 class="text-2xl sm:text-3xl xl:text-4xl font-bold leading-none">
        {{ hero.heading }}
      </h1>
      <p class="mt-4 text-sm sm:text-base text-indigo-100">{{ hero.subheading }}</p>
    </div>
  </section>
  <span class="w-full h-12 block gradient-purple text-gray-100">
    {% include 'icons/waves.svg' %}
  </span>

  <section class="container max-w-screen-lg mt-16">
    {% for site in collections.sites %}
      <article class="sm:flex items-center pt-12 mt-12 {{ 'border-t' if not loop.first }}">
        <div
          class="sm:w-1/3 h-64 bg-gray-400 bg-cover rounded-lg shadow-lg"
          style="background-image: url({{ site.data.image }})"
        ></div>
        <div class="mt-8 sm:ml-12 sm:mt-0 sm:w-2/3">
          <a target="_blank" href="{{ site.data.link }}">
            <h2 class="text-3xl font-extrabold leading-none">{{ site.data.title }}</h2>
            <p class="text-sm font-medium {{ site.data.color }} underline">{{ site.data.link }}</p>
          </a>
          <p class="text-base text-gray-700 mt-4">{{ site.data.description }}</p>
          <a
            target="_blank"
            href="{{ site.data.link }}"
            class="inline-flex items-center mt-6 {{ site.data.color }} font-bold"
          >
            <span class="leading-none">
              View Site
            </span>
            <span class="w-4 ml-1">
              {% include 'icons/arrow.svg' %}
            </span>
          </a>
        </div>
      </article>
    {% endfor %}
  </section>
{% endblock body %}
